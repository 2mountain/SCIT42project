<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- DAO인터페이스와 매핑 -->
<mapper namespace="net.softsociety.aimori.dao.AimoriDAO">
	<insert id="test" parameterType="string">
		insert into test
		(test)
		values
		(#{string})
	</insert>
</mapper>